hpc:
  host: your.hpc.server
  user: ${oc.env:USER}
  port: 22
sync:
  source_dir: ${hydra:runtime.cwd}
  target_dir: ~/mnt/parscratch/users/acp23crg/${hydra:runtime.cwd:name}
  exclude:
  - .git/
  - __pycache__/
  - '*.pyc'
  - .env
  - venv/
  - .pytest_cache/
  - .coverage
  - .ruff_cache/
  - '*.egg-info/'
  options:
  - --archive
  - --compress
  - --verbose
  - --delete
